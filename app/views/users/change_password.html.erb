<% content_for :title, "Cambiar Contraseña" %>

<div class="container-main">
  <div class="page-header">
    <h1 class="page-title">🔐 Cambiar Contraseña</h1>
    <p class="page-subtitle">Debes cambiar tu contraseña antes de continuar</p>
    <div style="margin: 1.5rem 0; padding: 1rem; background: #7f1d1d; border-left: 4px solid #ef4444; border-radius: 0.5rem; color: #fecaca;">
      <strong>⚠️ Contraseña Temporal Detectada</strong>
      <br>Por seguridad, debes establecer una nueva contraseña antes de acceder al sistema.
      <br><small>Tu contraseña actual es: <code style="background: #991b1b; padding: 0.2rem 0.5rem; border-radius: 0.25rem;">123456</code></small>
    </div>
  </div>

  <div class="form-container">
    <%= form_with model: @user, url: update_password_path, method: :patch, local: true do |f| %>
      <% if @user.errors.any? %>
        <div class="form-errors">
          <h3 class="error-title">❌ Se encontraron <%= @user.errors.count %> error(es):</h3>
          <ul class="error-list">
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :password, "Nueva Contraseña", class: "form-label" %>
        <%= f.password_field :password, class: "form-input", placeholder: "Mínimo 6 caracteres", autocomplete: "new-password" %>
        <small style="color: #9ca3af; font-size: 0.875rem;">⚠️ No puede ser la contraseña por defecto (123456)</small>
      </div>

      <div class="form-group">
        <%= f.label :password_confirmation, "Confirmar Nueva Contraseña", class: "form-label" %>
        <%= f.password_field :password_confirmation, class: "form-input", placeholder: "Repite la contraseña", autocomplete: "new-password" %>
      </div>

      <div class="form-actions">
        <%= f.submit "🔒 Cambiar Contraseña", class: "form-submit" %>
      </div>
    <% end %>
  </div>
</div>
